extends layout

block content
	hr
	h1.display-4.text-center Create better FiveM plugins faster with NFive
	.card-deck
		.card
			//img.card-img-top(src='.../100px180/', alt='Card image cap')
			.card-body
			h5.card-title.text-center Unique Plugins
			p.card-text.text-center #{uniquePlugins}
		.card
			//img.card-img-top(src='.../100px180/', alt='Card image cap')
			.card-body
			h5.card-title.text-center Plugin Installs
			p.card-text.text-center #{pluginInstalls}
		.card
			//img.card-img-top(src='.../100px180/', alt='Card image cap')
			.card-body
			h5.card-title.text-center Plugin Versions
			p.card-text.text-center #{pluginVersions}
	hr
	h1.display-5.text-center Most Installed Plugins
	hr
	h1.display-5.text-center What is NFive & hub.nfive.io?
	hr
	h1.display-5.text-center Create better FiveM plugins faster with NFive
